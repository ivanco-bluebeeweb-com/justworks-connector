# Justworks Connector — UI Component Plan

- **Left Sidebar:**
  - Standard container: `ui.Stack(direction="v", gap=3, align="stretch")`.
  - Form: `label`, `api_token` (placeholder "Enter Justworks API Token"), `base_url`.
  - Action button: `Connect Justworks`.
  - How to connect modal: `_help_modal()`.
  - Settings: `_settings_button()`.
